/*!
\page airtsp
      Binary executable for the C++ Simulated Airline Schedule Manager Library

\section sec_synopsis SYNOPSIS

<b>airtsp</b> <tt>[--prefix] [-v|--version] [-h|--help] [-b|--builtin] [-i|--input <path-to-input>] [-l|--log <path-to-output-log-file>]</tt>

\section sec_description DESCRIPTION

\e airtsp is a small program showing how to use the AirTSP library.

\e airtsp accepts the following options:

 \b --prefix<br>
    Show the AirTSP installation prefix.

 \b -v, \b --version<br>
    Print the currently installed version of AirTSP on the standard output.

 \b -h, \b --help<br>
    Produce that message and show usage.

 \b -b, \b --builtin<br>
    The sample BOM tree can be either built-in or parsed from input
    files. In that latter case, the -i/--input option must be
    specified as well

 \b -i, \b --input <path-to-input-file><br>
    Path (absolute or relative) of the (CSV) input file specifying
    the schedule (flight-period) entries.<br>

 \b -l, \b --log <path-to-output-log-file><br>
    Path (absolute or relative) of the output log file.<br>

 \b -r, \b --read_booking_request<br>
    Indicates that a booking request is given as a command-line option.
    That latter must then be given with the -b/--bkg_req option.<br>

 \b -q, \b --bkg_req <booking-request><br>
    Booking request word list (e.g. 'NCE BKK NCE 2007-04-21 2007-04-21
    10:00:00 C 1 DF RO 5 NONE 10:0:0 2000.0 20.0'), which should be
    located at the end of the command line (otherwise, the other
    options would be interpreted as part of that booking request word
    list).<br>


See the output of the <tt>`airtsp --help'</tt> command for default options.


\section sec_see_also SEE ALSO
\b airtsp-config(1), \b airtsp-library(3)


\section sec_support SUPPORT

Please report any bugs to https://sourceforge.net/apps/trac/airtsp/report


\section sec_copyright COPYRIGHT

Copyright © 2009-2013 Denis Arnaud

See the COPYING file for more information on the (LGPLv2+) license, or
directly on Internet:<br>
http://www.gnu.org/licenses/old-licenses/lgpl-2.1.html

*/
